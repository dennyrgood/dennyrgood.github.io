// Main Application Initialization

window.addEventListener('DOMContentLoaded', () => {
    // Initialize add form handler
    initAddForm();
    
    // Load initial data
    openTab('data-view');
});